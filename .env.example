# BudgetHub Family local configuration
# Copy to .env for local use. Do not commit real secrets.

APP_ENV=local
APP_BASE_URL=http://localhost:5173

# Optional public settings
DEFAULT_LANGUAGE=fr
DEFAULT_CURRENCY=CAD
